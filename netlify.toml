[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  command = "npx tailwindcss -i ./src/input.css -o ./src/index.css && vite build"
  publish = "dist"
